# wwts

Code released to Open Source from WorldWide Tech Services

Special thanks to Cindy Niccum and Scott McPerson for allowing me to release this code

## What we liberated

No business logic, but once ye got that coded: how to release it

How to `DevOps` it

As if you started in `bash`, then found `Python`, then `Ansible` let you do a Picard

> Make it so

## What we added

The full `OSS` version of the `WWTS` code recognises that `Claude` writes more code than I do

So I explained the code to `Claude` and it suggested some commands, skills, added collections, ...
